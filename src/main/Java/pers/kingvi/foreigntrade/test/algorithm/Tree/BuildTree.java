package pers.kingvi.foreigntrade.test.algorithm.Tree;

public class BuildTree {
    public void buildTree(TreeNode root, int[] arr, int i) {
        if ( i >= arr.length) {
            return;
        }
//        i++;
        TreeNode left = new TreeNode(arr[i]);
    }
}
